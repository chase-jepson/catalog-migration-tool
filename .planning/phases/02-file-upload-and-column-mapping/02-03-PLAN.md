---
phase: 02-file-upload-and-column-mapping
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - components/mapping/MappingStep.tsx
  - components/mapping/MappingGroup.tsx
  - components/mapping/MappingRow.tsx
  - components/mapping/MappingToolbar.tsx
  - components/mapping/DataPreview.tsx
  - components/wizard/WizardShell.tsx
autonomous: false
requirements: [MAP-01, MAP-02]

must_haves:
  truths:
    - "User sees column mappings grouped by category (Product Info, Cannabis Details, Pricing, Attributes, Display & Media)"
    - "Auto-mapped fields are pre-selected from POS-specific templates"
    - "User can override any mapping via dropdown of source columns"
    - "Each mapping row shows a sample value preview from the CSV data"
    - "Unmapped required fields have red outline and group headers show unmapped count badge"
    - "User cannot proceed to Review until all required fields are mapped"
    - "Clear All removes all mappings; Reset to Auto re-applies POS defaults"
    - "Collapsible Preview Data section shows horizontally scrollable table at bottom of Map step"
    - "Column headers in preview are color-coded: green for mapped, yellow/gray for unmapped"
    - "Clicking a column header in preview scrolls to the corresponding mapping row"
  artifacts:
    - path: "components/mapping/MappingStep.tsx"
      provides: "Map step container with grouped mapping rows, toolbar, and data preview"
      min_lines: 60
    - path: "components/mapping/MappingGroup.tsx"
      provides: "Collapsible group of mapping rows with header showing group name and unmapped count"
      min_lines: 40
    - path: "components/mapping/MappingRow.tsx"
      provides: "Single mapping row: target field label, source column dropdown, sample value preview"
      min_lines: 30
    - path: "components/mapping/MappingToolbar.tsx"
      provides: "Clear All and Reset to Auto buttons"
      min_lines: 15
    - path: "components/mapping/DataPreview.tsx"
      provides: "Collapsible preview table with color-coded headers and click-to-scroll"
      min_lines: 50
  key_links:
    - from: "components/mapping/MappingStep.tsx"
      to: "lib/mapping-engine.ts"
      via: "calls getMappingsByGroup, updateMapping, applyPOSDefaults, getUnmappedRequired, getSampleValue"
      pattern: "import.*from.*mapping-engine"
    - from: "components/mapping/MappingStep.tsx"
      to: "components/mapping/DataPreview.tsx"
      via: "renders DataPreview with merged rows and mapping state"
      pattern: "DataPreview"
    - from: "components/wizard/WizardShell.tsx"
      to: "components/mapping/MappingStep.tsx"
      via: "renders MappingStep for step 1"
      pattern: "MappingStep"
---

<objective>
Build the Map step UI: grouped column mapping interface with sample previews, mapping toolbar (Clear All / Reset to Auto), collapsible data preview table with color-coded headers, and scroll-to-mapping interaction. Wire into WizardShell as step 1.

Purpose: Delivers the second wizard step -- users see and adjust how their POS columns map to Treez fields, with visual feedback and data preview.
Output: Working Map step with full mapping UI, integrated into wizard with canProceed gating. Human verification of complete Upload + Map flow.
</objective>

<execution_context>
@/Users/chase/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chase/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-file-upload-and-column-mapping/02-CONTEXT.md
@.planning/phases/02-file-upload-and-column-mapping/02-RESEARCH.md
@.planning/phases/02-file-upload-and-column-mapping/02-01-SUMMARY.md
@.planning/phases/02-file-upload-and-column-mapping/02-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
From lib/types.ts:
```typescript
export type MappingGroup = 'Product Info' | 'Cannabis Details' | 'Pricing' | 'Attributes' | 'Display & Media';
export interface FieldMapping { fieldKey: string; label: string; sourceHeader: string | null; }
export interface ParsedFile { fileName: string; fileSize: number; headers: string[]; rows: Record<string, string>[]; rowCount: number; previewRows: Record<string, string>[]; }
```

From lib/mapping-engine.ts:
```typescript
export function applyPOSDefaults(posName: string): FieldMapping[];
export function updateMapping(mappings: FieldMapping[], fieldKey: string, sourceHeader: string | null): FieldMapping[];
export function clearAllMappings(mappings: FieldMapping[]): FieldMapping[];
export function getUnmappedRequired(): string[];
export function getMappingsByGroup(mappings: FieldMapping[]): Record<MappingGroup, FieldMapping[]>;
export function getSampleValue(rows: Record<string, string>[], header: string): string;
```

From lib/constants.ts:
```typescript
export const MAPPING_GROUPS: MappingGroup[];
export const MAPPING_FIELDS: MappingFieldDef[];
```

<!-- From Plan 02 -->
WizardShell state (components/wizard/WizardShell.tsx):
```typescript
// WizardShell manages: parsedFiles, mergedFile, selectedPOS, detectedPOS, mappings, canProceed, currentStep
// Step components receive state via props and call callbacks to update
// canProceed for step 1 (Map) = all required fields mapped
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mapping step components</name>
  <files>components/mapping/MappingRow.tsx, components/mapping/MappingGroup.tsx, components/mapping/MappingToolbar.tsx, components/mapping/DataPreview.tsx, components/mapping/MappingStep.tsx</files>
  <action>
    1. Create `components/mapping/MappingRow.tsx`:
       - Props: `fieldKey: string`, `label: string`, `description: string`, `required: boolean`, `sourceHeader: string | null`, `sourceColumns: string[]`, `sampleValue: string`, `onMappingChange(fieldKey: string, sourceHeader: string | null): void`, `highlighted?: boolean`
       - Layout: target field label on left, source column `<select>` dropdown on right, sample value preview below the dropdown (small gray text showing first non-empty value from that column)
       - Dropdown options: empty option ("-- Select column --") + all source columns from the parsed file
       - If `required && !sourceHeader`: red outline (`ring-2 ring-red-400`) on the dropdown
       - If `highlighted`: briefly flash/highlight background (use CSS transition, teal-50 bg)
       - Use `useRef` to expose element for scroll-to from DataPreview
       - Add `id={`mapping-row-${fieldKey}`}` for scroll targeting

    2. Create `components/mapping/MappingGroup.tsx`:
       - Props: `groupName: MappingGroup`, `mappings: FieldMapping[]`, `sourceColumns: string[]`, `rows: Record<string, string>[]`, `onMappingChange(fieldKey: string, sourceHeader: string | null): void`, `highlightedField?: string`
       - Collapsible section with chevron icon in header
       - Header shows: group name, unmapped required count badge (e.g., "2 unmapped" in red if > 0, green checkmark if all mapped)
       - Default state: expanded if any unmapped required fields, collapsed if all mapped
       - Renders MappingRow for each visible (non-hidden) field in the group
       - Pass getSampleValue(rows, mapping.sourceHeader) to each MappingRow for preview
       - Claude's discretion on collapse animation (simple height transition or instant toggle)

    3. Create `components/mapping/MappingToolbar.tsx`:
       - Props: `onClearAll(): void`, `onResetToAuto(): void`, `posName: string`
       - Two buttons: "Clear all" (gray/outline style) and "Reset to auto" (teal outline)
       - "Reset to auto" label includes POS name: "Reset to {POS} defaults"
       - Compact horizontal bar above the mapping groups

    4. Create `components/mapping/DataPreview.tsx`:
       - Props: `mergedFile: ParsedFile`, `mappings: FieldMapping[]`, `onColumnClick(header: string): void`
       - Collapsible "Preview Data" section -- collapsed by default, toggle with chevron
       - When expanded: horizontally scrollable table (`overflow-x: auto` wrapper around `<table>`)
       - Show first N rows (Claude's discretion -- recommend 5-10 rows for side panel width)
       - Column headers color-coded: if header is mapped to a target field -> green tint (`bg-green-50 text-green-800`), if unmapped -> yellow/gray tint (`bg-gray-100 text-gray-500`)
       - Clicking a column header: call `onColumnClick(header)` which will scroll to and highlight the corresponding MappingRow above
       - Table cells: text-sm, truncated with ellipsis for long values, white background

    5. Create `components/mapping/MappingStep.tsx`:
       - Props: `mappings: FieldMapping[]`, `onMappingsChange(mappings: FieldMapping[]): void`, `mergedFile: ParsedFile`, `selectedPOS: string`, `onCanProceed(canProceed: boolean): void`
       - On mount and on mappings change: compute `canProceed = getUnmappedRequired(mappings).length === 0` and call onCanProceed
       - Track `highlightedField` state for scroll-to-mapping from DataPreview
       - `onColumnClick(header)`: find which fieldKey maps to this header, set highlightedField, scroll element into view using `document.getElementById('mapping-row-{fieldKey}')?.scrollIntoView({ behavior: 'smooth', block: 'center' })`
       - Clear highlight after 2 seconds with setTimeout
       - Renders: MappingToolbar (top), then MAPPING_GROUPS.map -> MappingGroup for each, then DataPreview (bottom)
       - onClearAll: call clearAllMappings, update mappings
       - onResetToAuto: call applyPOSDefaults(selectedPOS), update mappings
  </action>
  <verify>
    <automated>pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>All 5 mapping components created: MappingRow with dropdown and sample preview, MappingGroup with collapsible sections and unmapped badge, MappingToolbar with Clear All and Reset to Auto, DataPreview with color-coded headers and click-to-scroll, MappingStep orchestrating everything. Extension builds.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Map step into WizardShell and canProceed gating</name>
  <files>components/wizard/WizardShell.tsx</files>
  <action>
    1. Update `components/wizard/WizardShell.tsx`:
       - Import MappingStep from components/mapping/MappingStep
       - Replace StepPlaceholder for step 1 (Map) with MappingStep
       - Pass to MappingStep: mappings, onMappingsChange, mergedFile, selectedPOS, onCanProceed
       - Keep StepPlaceholder for steps 2 (Review) and 3 (Import)
       - canProceed is already tracked in state from Plan 02 -- ensure it updates correctly when user switches between steps (each step sets its own canProceed)
       - When selectedPOS changes (from Upload step), auto-apply POS defaults to mappings if mappings are currently all empty (first time). If mappings have been manually edited, do NOT auto-apply -- respect user's work.
       - Persist mappings changes to migration store (already debounced from Plan 02)

    2. Ensure Back button from Map step returns to Upload step correctly, and state is preserved (files, POS selection, mappings all stay in state).

    3. Ensure Next button on Map step is disabled until all required fields are mapped (canProceed from MappingStep).
  </action>
  <verify>
    <automated>pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>Map step renders in wizard at step 1 with grouped mapping UI. canProceed gates Next button. Back/Next navigation preserves state. Mappings persist via migration store.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete Upload and Map wizard flow</name>
  <what-built>Complete Upload and Map wizard flow: file upload with drag-and-drop, multi-file support, POS auto-detection, grouped column mapping UI with sample previews, data preview table, and state persistence.</what-built>
  <how-to-verify>
    1. Build and load the extension: `pnpm build` then load `.output/chrome-mv3` in Chrome
    2. Navigate to a Treez import page (any environment)
    3. Click "Migrate Catalog" button -- side panel opens with Upload step
    4. Test file upload:
       a. Click "Choose file" and select a CSV -- should parse and show summary card
       b. Verify POS is auto-detected (use a Dutchie or Blaze export if available, or any CSV)
       c. Click "Change" on POS label and select a different POS
       d. If you have an XLSX file with multiple sheets, verify sheet selector appears
    5. Click Next to go to Map step:
       a. Verify mapping groups are shown (Product Info, Cannabis Details, Pricing, Attributes, Display & Media)
       b. Verify auto-mapped fields have source columns pre-selected
       c. Verify unmapped required fields have red outlines
       d. Change a mapping via dropdown -- verify it updates
       e. Click "Clear all" -- all mappings should reset to empty
       f. Click "Reset to {POS} defaults" -- mappings should re-populate
       g. Expand "Preview Data" at bottom -- verify table shows with color-coded headers
       h. Click a column header in preview -- should scroll to the corresponding mapping row
    6. Test persistence: close and reopen the side panel -- state should be restored
    7. Test Next button gating: unmapped required fields should prevent proceeding
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 2, or describe any issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm build` -- extension builds without errors
- `pnpm vitest run` -- all tests pass
- Full manual test of Upload -> Map flow in Chrome extension
- State persists across side panel close/reopen
- canProceed properly gates navigation
</verification>

<success_criteria>
- Map step shows grouped column mappings with auto-populated POS defaults
- User can override any mapping via dropdown
- Sample values shown next to each mapping
- Unmapped required fields highlighted with red outline and count badge
- Clear All and Reset to Auto toolbar buttons work correctly
- Data preview table shows with color-coded headers (green=mapped, gray=unmapped)
- Clicking preview column header scrolls to corresponding mapping row
- Next button disabled until all required fields mapped
- State persists in chrome.storage.local
- Human verifies end-to-end Upload + Map flow in Chrome
</success_criteria>

<output>
After completion, create `.planning/phases/02-file-upload-and-column-mapping/02-03-SUMMARY.md`
</output>
