---
phase: 03-transform-validate-and-import
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/types.ts
  - lib/category-mapper.ts
  - lib/transformer.ts
  - tests/category-mapper.test.ts
  - tests/transformer.test.ts
autonomous: true
requirements: [XFRM-01, XFRM-02, XFRM-03, VAL-03]

must_haves:
  truths:
    - "Source categories are normalized to one of 14 Treez product categories using 40+ keyword rules"
    - "Subcategories are resolved per-category using product name keyword matching"
    - "Weight values are parsed and converted to category-expected UoM (grams, milligrams, each)"
    - "Classification values are normalized to one of 6 valid values (Sativa, Indica, Hybrid, I/S, S/I, CBD)"
    - "Category change cascades to subCategory, UoM, and merchSize updates via applyFixes"
  artifacts:
    - path: "lib/types.ts"
      provides: "DerivedRow, CategoryResolution, RowFix, TransformResult types"
      contains: "DerivedRow"
    - path: "lib/category-mapper.ts"
      provides: "Category and subcategory resolution from source fields"
      exports: ["resolveCategory", "resolveSubCategory"]
      min_lines: 400
    - path: "lib/transformer.ts"
      provides: "Row derivation, weight/classification normalization, fix application"
      exports: ["deriveRows", "applyFixes", "normalizeClassification", "parseWeight"]
      min_lines: 300
    - path: "tests/category-mapper.test.ts"
      provides: "Category mapper unit tests"
      min_lines: 80
    - path: "tests/transformer.test.ts"
      provides: "Transformer unit tests"
      min_lines: 80
  key_links:
    - from: "lib/transformer.ts"
      to: "lib/category-mapper.ts"
      via: "import resolveCategory, resolveSubCategory"
      pattern: "import.*category-mapper"
    - from: "lib/transformer.ts"
      to: "lib/types.ts"
      via: "import DerivedRow, FieldMapping"
      pattern: "import.*types"
---

<objective>
Port the v1 transformation pipeline (category mapper + transformer) to v2 with comprehensive unit tests. This is the pure logic foundation that ReviewStep and ImportStep will consume.

Purpose: All data normalization logic must be in place and tested before building the UI that displays and edits transformed data.
Output: `lib/category-mapper.ts`, `lib/transformer.ts`, extended `lib/types.ts`, and passing test suites.
</objective>

<execution_context>
@/Users/chase/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chase/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-transform-validate-and-import/03-CONTEXT.md
@.planning/phases/03-transform-validate-and-import/03-RESEARCH.md

@lib/types.ts
@lib/constants.ts
@lib/mapping-engine.ts

<!-- V1 source files to port from -->
@/Users/chase/projects/chrome-extension/src/lib/categoryMapper.ts
@/Users/chase/projects/chrome-extension/src/lib/transformer.ts
@/Users/chase/projects/chrome-extension/src/types/index.ts

<interfaces>
<!-- Existing v2 types the transformer must consume -->
From lib/types.ts:
```typescript
export interface ParsedFile {
  fileName: string; fileSize: number; headers: string[];
  rows: Record<string, string>[]; rowCount: number;
  previewRows: Record<string, string>[]; sourceFileName?: string;
}
export interface FieldMapping {
  fieldKey: string; label: string; sourceHeader: string | null;
}
```

From lib/constants.ts:
```typescript
export const PRODUCT_CATEGORIES = ['Beverage','CBD','Cartridge','Edible','Extract','Flower','Merch','Misc','Non-Inv','Pill','Plant','Preroll','Tincture','Topical'] as const;
export type ProductCategory = (typeof PRODUCT_CATEGORIES)[number];
export const PRODUCT_SUBCATEGORIES: Record<string, string[]> = { /* per-category arrays */ };
export const UOM_BY_CATEGORY: Record<string, string> = { /* category -> grams|milligrams|each */ };
export const EACH_UOM_CATEGORIES = new Set(['Merch', 'Non-Inv', 'Plant']);
export const VALID_CLASSIFICATIONS = ['Sativa','Indica','Hybrid','I/S','S/I','CBD'] as const;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Phase 3 types and port category mapper with tests</name>
  <files>lib/types.ts, lib/category-mapper.ts, tests/category-mapper.test.ts</files>
  <behavior>
    - resolveCategory('Flower', '', 'Blue Dream Pre-Roll') returns { category: 'Flower', ... }
    - resolveCategory('Gummy', '', 'Sour Gummies 10mg') returns { category: 'Edible', ... }
    - resolveCategory('Vape Cartridge', '', '510 Cart') returns { category: 'Cartridge', ... }
    - resolveCategory('', '', 'Unknown Product') returns { category: 'Misc', ... } (fallback)
    - resolveSubCategory('Flower', 'Blue Dream Pre-Roll') returns a valid Flower subcategory
    - resolveSubCategory('Edible', 'Sour Gummy Bears') returns 'Gummy'
    - Each of the 14 Treez categories has at least one keyword that resolves to it
  </behavior>
  <action>
1. Extend `lib/types.ts` with new types needed for Phase 3. Port from v1 `types/index.ts` and adapt:
   - `DerivedRow`: all 25+ fields for the Treez import row (productName, brand, category, subCategory, classification, amount, uom, basePrice, status, description, menuTitle, strain, thc, cbd, tags, effects, flavor, ingredients, imageFilename, availableOnline, priceTier, priceType, merchSize, productIdentifier, variantIdentifier)
   - `CategoryResolution`: { category: ProductCategory; subCategory: string; uom: string; merchSize: string }
   - `RowFix`: { rowIndex: number; field: string; newValue: string } -- for inline error corrections
   - `TransformResult`: { derivedRows: DerivedRow[]; categoryResolutions: Map<string, CategoryResolution> }

2. Write failing tests in `tests/category-mapper.test.ts` covering:
   - At least one keyword match per Treez category (14 categories)
   - Combined field matching (category + externalCategory + productName)
   - Name-based fallback when category fields are empty
   - Subcategory resolution per category

3. Port `lib/category-mapper.ts` from v1's `categoryMapper.ts` (655 lines). Adapt to v2:
   - Export `resolveCategory(categoryField: string, externalCategory: string, productName: string): CategoryResolution`
   - Export `resolveSubCategory(category: string, productName: string): string`
   - Keep ALL 40+ keyword rules as-is (battle-tested, per user decision)
   - Use constants from `lib/constants.ts` (PRODUCT_CATEGORIES, PRODUCT_SUBCATEGORIES, UOM_BY_CATEGORY, EACH_UOM_CATEGORIES)
   - Remove any v1-specific imports (replace with v2 equivalents)

4. Run tests, verify all pass.
  </action>
  <verify>
    <automated>npx vitest run tests/category-mapper.test.ts -x</automated>
  </verify>
  <done>All 14 Treez categories have keyword rules that resolve correctly. Subcategory resolution works per-category. Tests pass covering core paths and edge cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port transformer (deriveRows, weight/classification normalization, applyFixes) with tests</name>
  <files>lib/transformer.ts, tests/transformer.test.ts</files>
  <behavior>
    - deriveRows with mapped fields produces DerivedRow[] with normalized category, subCategory, uom, classification
    - parseWeight('0.5g') returns { amount: 0.5, unit: 'grams' }
    - parseWeight('500mg') returns { amount: 500, unit: 'milligrams' }
    - parseWeight('1oz') returns { amount: 28.3495, unit: 'grams' } (converted)
    - parseWeight('unparseable') returns { amount: 0, unit: '' } (flagged as error per user decision)
    - normalizeClassification('SATIVA') returns 'Sativa'
    - normalizeClassification('hybrid') returns 'Hybrid'
    - normalizeClassification('I/S') returns 'I/S'
    - normalizeClassification('unknown') returns '' (empty, caught by validator)
    - applyFixes changes category -> cascades to subCategory, uom, merchSize
    - deriveRows handles 10k+ rows without hanging (< 200ms)
  </behavior>
  <action>
1. Write failing tests in `tests/transformer.test.ts` covering:
   - deriveRows: maps source row fields to DerivedRow using FieldMapping[]
   - parseWeight: handles g, mg, oz, plain numbers, unparseable values
   - normalizeClassification: case-insensitive matching, abbreviations, invalid values
   - applyFixes: category change cascades (category -> subCategory, uom, merchSize recalculated)
   - Performance: deriveRows on 1000 rows completes in < 500ms

2. Port `lib/transformer.ts` from v1's `transformer.ts` (1014 lines). This file is large so focus on:
   - `deriveRows(rows: Record<string, string>[], mappings: FieldMapping[]): TransformResult` -- the core transformation
   - `parseWeight(value: string): { amount: number; unit: string }` -- weight normalization
   - `normalizeClassification(value: string): string` -- classification normalization
   - `applyFixes(derivedRows: DerivedRow[], fixes: RowFix[]): DerivedRow[]` -- fix application with cascade logic
   - `getWeightInGrams(amount: number, unit: string): number` -- unit conversion helper
   - `extractGramsFromName(name: string): number` -- fallback weight extraction from product name
   - `convertAmount(amount: number, fromUnit: string, toCategory: string): number` -- cross-unit conversion
   - Import and use `resolveCategory`, `resolveSubCategory` from `lib/category-mapper.ts`
   - Use `VALID_CLASSIFICATIONS`, `UOM_BY_CATEGORY`, `EACH_UOM_CATEGORIES` from constants
   - Weight parsing strategy for unparseable values: flag as error (amount = 0) per research recommendation (matches v1 behavior)
   - Do NOT port `buildOutputCSVs` -- that goes in Plan 02's csv-generator.ts

3. Run tests, verify all pass.
  </action>
  <verify>
    <automated>npx vitest run tests/transformer.test.ts -x</automated>
  </verify>
  <done>deriveRows transforms source rows to DerivedRow[] with normalized categories, weights, and classifications. applyFixes cascades category changes correctly. All tests pass.</done>
</task>

</tasks>

<verification>
```bash
npx vitest run tests/category-mapper.test.ts tests/transformer.test.ts -x
```
All transformation logic tests pass. Types compile without errors.
</verification>

<success_criteria>
- lib/types.ts extended with DerivedRow, CategoryResolution, RowFix, TransformResult
- lib/category-mapper.ts has 40+ keyword rules ported from v1, resolves all 14 categories
- lib/transformer.ts has deriveRows, parseWeight, normalizeClassification, applyFixes
- All unit tests pass
- No v1-specific imports remain (all adapted to v2 constants/types)
</success_criteria>

<output>
After completion, create `.planning/phases/03-transform-validate-and-import/03-01-SUMMARY.md`
</output>
