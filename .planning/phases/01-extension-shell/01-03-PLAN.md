---
phase: 01-extension-shell
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - entrypoints/sidepanel/main.tsx
  - entrypoints/sidepanel/App.tsx
  - components/wizard/WizardShell.tsx
  - components/wizard/StepIndicator.tsx
  - components/wizard/StepPlaceholder.tsx
autonomous: false
requirements:
  - EXT-01

must_haves:
  truths:
    - "Side panel renders the correct wizard type based on which button was clicked"
    - "Wizard shows 4 steps: Upload, Map, Review, Import"
    - "User can navigate forward and backward through wizard steps"
    - "Step indicator highlights the current step"
    - "Back button is disabled on step 1, Next button is disabled on step 4"
  artifacts:
    - path: "entrypoints/sidepanel/App.tsx"
      provides: "Root side panel component that reads wizard type from storage"
      contains: "wizardType"
    - path: "components/wizard/WizardShell.tsx"
      provides: "4-step wizard container with navigation"
      exports: ["WizardShell"]
    - path: "components/wizard/StepIndicator.tsx"
      provides: "Visual step progress indicator"
      exports: ["StepIndicator"]
    - path: "components/wizard/StepPlaceholder.tsx"
      provides: "Empty placeholder for future step content"
      exports: ["StepPlaceholder"]
  key_links:
    - from: "entrypoints/sidepanel/App.tsx"
      to: "chrome.storage.session"
      via: "reads wizardType set by background"
      pattern: "storage\\.session\\.get"
    - from: "components/wizard/WizardShell.tsx"
      to: "components/wizard/StepIndicator.tsx"
      via: "renders StepIndicator with current step"
      pattern: "StepIndicator.*current"
    - from: "components/wizard/WizardShell.tsx"
      to: "components/wizard/StepPlaceholder.tsx"
      via: "renders placeholder for current step content"
      pattern: "StepPlaceholder"
---

<objective>
Build the side panel wizard UI with step navigation. The side panel reads which wizard type was requested (catalog or inventory) and presents a 4-step wizard shell with placeholder content for each step.

Purpose: Completes the extension shell -- user clicks a button, side panel opens with the correct wizard, and they can navigate through empty steps. This is the UI framework that Phase 2+ fills with real content.
Output: Working side panel with wizard navigation, step indicator, and placeholder steps.
</objective>

<execution_context>
@/Users/chase/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chase/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-extension-shell/01-CONTEXT.md
@.planning/phases/01-extension-shell/01-RESEARCH.md
@.planning/phases/01-extension-shell/01-01-SUMMARY.md
@.planning/phases/01-extension-shell/01-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->

From lib/constants.ts:
```typescript
export const STEP_LABELS: string[];  // ['Upload', 'Map', 'Review', 'Import']
```

<!-- From Plan 02 -- background stores wizardType in session storage -->

Background script (entrypoints/background/index.ts) stores:
```typescript
// In openSidePanel handler:
await chrome.storage.session.set({ wizardType: data.wizardType });
chrome.sidePanel.open({ tabId });
```

Side panel reads wizardType via:
```typescript
const { wizardType } = await chrome.storage.session.get('wizardType');
// wizardType: 'catalog' | 'inventory'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wizard shell components and side panel wiring</name>
  <files>entrypoints/sidepanel/main.tsx, entrypoints/sidepanel/App.tsx, components/wizard/WizardShell.tsx, components/wizard/StepIndicator.tsx, components/wizard/StepPlaceholder.tsx</files>
  <action>
1. Create `components/wizard/StepIndicator.tsx`:
   - Props: `{ steps: string[]; current: number }`
   - Render a horizontal stepper showing all step labels
   - Current step is visually highlighted (bold text, colored indicator)
   - Completed steps (index < current) show a check or filled indicator
   - Future steps are dimmed/muted
   - Use Tailwind classes for styling: flex row, gap between steps, colored dots/circles with step numbers
   - Keep it clean and minimal -- this is a migration tool, not a marketing page

2. Create `components/wizard/StepPlaceholder.tsx`:
   - Props: `{ stepName: string }`
   - Render a centered placeholder message: "Step: {stepName}" with a subtitle "Coming in Phase 2" or similar
   - Light gray background or dashed border to make it clear this is a placeholder
   - Use Tailwind utility classes

3. Create `components/wizard/WizardShell.tsx`:
   - Props: `{ wizardType: 'catalog' | 'inventory' }`
   - State: `currentStep` (number, 0-indexed, default 0)
   - Layout: flex column, full height
   - Header: title showing "Migrate Catalog" or "Migrate Inventory" based on wizardType, plus StepIndicator
   - Main content area: flex-1, overflow-auto, renders StepPlaceholder for current step
   - Footer: Back/Next buttons with proper disabled states
     - Back disabled when currentStep === 0
     - Next disabled when currentStep === 3 (last step)
     - Buttons styled as primary (Next) and secondary (Back)
   - Follow the pattern from RESEARCH.md "Wizard Shell Component" example but add proper Tailwind styling

4. Create `entrypoints/sidepanel/App.tsx`:
   - On mount, read `wizardType` from `chrome.storage.session.get('wizardType')`
   - Use React state to store wizardType, show a loading state while reading
   - If wizardType is null/undefined, show a message: "Click 'Migrate Catalog' or 'Migrate Inventory' on the import page to start"
   - If wizardType exists, render `<WizardShell wizardType={wizardType} />`
   - Listen for storage changes (`chrome.storage.session.onChanged`) to update if wizardType changes (user clicks different button while panel is open)

5. Create `entrypoints/sidepanel/main.tsx`:
   - Import React, ReactDOM
   - Import `./style.css` for Tailwind
   - Mount `<App />` into `document.getElementById('root')`
   - Standard React 19 createRoot pattern

6. Verify build: `pnpm wxt build` should succeed with all three entrypoints (background, content script, sidepanel).
  </action>
  <verify>
    <automated>cd /Users/chase/projects/catalog-migration-tool && pnpm wxt build 2>&1 | tail -20</automated>
  </verify>
  <done>Side panel renders wizard with step indicator. WizardShell shows correct title per wizard type. Step navigation works (Back/Next with proper disabled states). Extension builds with all entrypoints.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete extension shell in Chrome</name>
  <files>none</files>
  <action>
Human verification of the complete extension shell. Build the extension with `pnpm wxt build` and instruct the user to load it in Chrome for manual testing.
  </action>
  <verify>
    <automated>cd /Users/chase/projects/catalog-migration-tool && pnpm wxt build 2>&1 | tail -5</automated>
  </verify>
  <done>User confirms: buttons appear on import page, side panel opens with correct wizard, step navigation works, side panel is restricted to import pages only.</done>
</task>

</tasks>

<verification>
- `pnpm wxt build` succeeds with all entrypoints
- `pnpm vitest run` -- all existing tests still pass
- Extension loads in Chrome without errors in the extension service worker console
- Side panel renders wizard UI with step navigation
</verification>

<success_criteria>
- Extension injects "Migrate Catalog" and "Migrate Inventory" buttons on Treez import page (EXT-01)
- Clicking a button opens the side panel with the correct wizard type
- Wizard shows 4-step navigation with working Back/Next
- Side panel is only available on import pages, not all Treez pages
- Extension works on production, sandbox, and dev Treez environments (EXT-03)
- Auth token reading is wired up (EXT-02) -- verified by no console errors when tokens exist
</success_criteria>

<output>
After completion, create `.planning/phases/01-extension-shell/01-03-SUMMARY.md`
</output>
